%table.table.table-striped
  %tbody
    - if @experiments.blank?
      %tr
        %td= t '.nothing'
    - else
      - @experiments.each do |e|

        - eid = e[:id]
        %tr
          %td
            %h4= link_to eid, experiment_path(eid)
            %p
              <strong>Owner</strong>: #{link_to e[:owner][:name], profile_path(e[:owner][:uid])} (#{e[:owner][:uid]})<br/>
              <strong>Description</strong>: #{e[:description]}<br/>
              <strong>Status</strong>: TBD<br/>
