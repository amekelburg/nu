matt:
  pending_projects:
    project:
      header: "Name: %{project_id}"
      leader: "Project Leader: %{leader}"
      approve: Approve
      reject: Reject

    index:
      title: New Project Requests
      info: >
        <p>
          Review and accept or reject the request
        </p>

      recent_activity: Recent Activity

    approve:
      success: Project has been approved
      failure: Failed to approve the project
    reject:
      success: Project has been rejected
      failure: Failed to reject the project
